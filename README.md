# 舊飛行場 airfields

1945 年台灣與澎湖日軍飛行場的純靜態單頁地圖：點一座，切到衛星影像，看它今天變成了什麼。
資料全在 `data/airfields.json`，地圖點、數字、篩選與清單都由瀏覽器端算出（Leaflet，零建置）。
網址 https://airfields.skyfaring.net （Cloudflare Pages，Git 連動，push `main` 即上線）；維護細節見 `CLAUDE.md`。
